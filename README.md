# Synapse
Synapse is a visual AI pipeline builder. Drag node-shaped processors onto a canvas, wire them up, and ship a working workflow in minutes. A FastAPI backend validates the graph with DAG and missing-input checks, then runs each step in topological order. Built with React Flow, Zustand, Tailwind, and FastAPI.
